%h1 Modifier un article
%br

.card
  = form_for :post, :url => {:controller => 'posts', :action => 'update'} do |f|
    .card-header
      %h5.card-title
        = f.text_field :title, class: 'form-control', placeholder: 'Titre de l\'article'
    .card-body
      %p.card-text
        = f.text_area :content, class: 'tinymce', placeholder: 'Contenu de l\'article'
    .card-footer
      = link_to 'Annuler la modification', posts_path, class: 'btn btn-danger text-white'
      = f.submit 'Modifier l\'article', class: 'btn btn-success text-white'